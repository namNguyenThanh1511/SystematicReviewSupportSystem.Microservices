apiVersion: v1
kind: Service
metadata:
  name: srss-project-service
  namespace: amayakuro

spec:
  type: ClusterIP
  ipFamilyPolicy: RequireDualStack
  selector:
    srss: project
  ports:
    - port: 80 
      targetPort: 8080
      name: srss-project