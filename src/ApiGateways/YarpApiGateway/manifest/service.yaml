apiVersion: v1
kind: Service
metadata:
  name: srss-service
  namespace: amayakuro

spec:
  type: ClusterIP
  ipFamilyPolicy: RequireDualStack
  selector:
    srss: gateway
  ports:
    - port: 80 
      targetPort: 8080
      name: srss-gateway